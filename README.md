# Mosters Café - LL 2026

Streamlit-app til vagtplanen.

## Kør lokalt
```bash
pip install -r requirements.txt
streamlit run app.py
```

## Deploy
Upload `app.py`, `vagter.csv`, `requirements.txt` og `logo.png` til et GitHub-repository og deploy på Streamlit Community Cloud.
